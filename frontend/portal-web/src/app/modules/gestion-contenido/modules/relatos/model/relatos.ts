export interface relato{
      usuario_id: string,
      relato_id: string,
      titulo: string,
      contenido: string,
      likes: [string],
      fecha_subida: string,
      aceptado: boolean
}

